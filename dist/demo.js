// Generated by CoffeeScript 2.3.1
(function() {
  //* ======================================== *#
  //* Usage
  var $, Semantics;

  Semantics = require('main');

  $ = Semantics();

  $.addDict('es-US', {
    plurals: {
      people: function(count, ...context) {
        switch (count) {
          case 1:
            return "Una persona";
          default:
            return `${count} personas`;
        }
      }
    },
    values: {
      hi: function(name) {
        return `¡Hola, ${name}!`;
      }
    }
  });

  $.addDict('en-US', {
    plurals: {
      people: function(count, ...context) {
        switch (count) {
          case 1:
            return "One person";
          default:
            return `${count} people`;
        }
      }
    },
    values: {
      hi: function(name) {
        return `Hi, ${name}!`;
      }
    }
  });

  console.log([
    $.setLang('es-US'), // Language set to Spanish
    $(1,
    'people'), // -> "Una persona"
    $(2,
    'people'), // -> "2 personas"
    $('hi',
    'Alice'), // -> "¡Hola, Alice!"
    $.setLang('es-US'), // Language set to English
    $(1,
    'people'), // -> "One person"
    $(2,
    'people'), // -> "2 people"
    $('hi',
    'Bob') // -> "Hi, Bob!"
  ]);

}).call(this);
